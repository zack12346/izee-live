import Link from "next/link";
import { BarChart3, ClipboardList, FolderOpen, Package, Plus, Settings, Ticket } from "lucide-react";
import { requireAdmin } from "@/lib/auth";
import { Button } from "@/components/ui/button";
export default async function AdminPage() { await requireAdmin(); const links = [{ href: "/admin/products", label: "إدارة المنتجات", icon: Package }, { href: "/admin/categories", label: "التصنيفات", icon: FolderOpen }, { href: "/admin/orders", label: "الطلبات والدفع", icon: ClipboardList }, { href: "/admin/coupons", label: "الكوبونات", icon: Ticket }, { href: "/admin/analytics", label: "التحليلات", icon: BarChart3 }, { href: "/admin/settings/payments", label: "إعدادات الدفع", icon: Settings }]; return <main className="container-shell section-space"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">إدارة Izée live</p><h1 className="mt-2 text-4xl font-black">لوحة التحكم</h1></div><Button className="bg-amber-400 text-black hover:bg-amber-300" render={<Link href="/admin/products?action=add" />}><Plus /> إضافة منتج</Button></div><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{links.map(({ href, label, icon: Icon }) => <Button key={href} variant="outline" className="h-auto justify-start gap-4 p-6 text-right" render={<Link href={href} />}><Icon className="text-primary" /><span>{label}</span></Button>)}</div></main>; }
