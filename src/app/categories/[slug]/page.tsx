import { notFound } from "next/navigation";
import { getCategoryBySlug, getProducts } from "@/services/catalog";
import { ProductCard } from "@/components/product-card";
export default async function CategoryPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const category = await getCategoryBySlug(slug); if (!category) notFound(); const result = await getProducts({ categoryId: category.id }); return <main className="container-shell section-space"><p className="eyebrow">التصنيف</p><h1 className="mt-2 text-4xl font-black">{category.name}</h1><p className="mt-4 max-w-2xl text-muted-foreground">{category.description}</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{result.products.map((product) => <ProductCard key={product.id} product={product} />)}</div>{!result.products.length ? <p className="mt-8 rounded-2xl border border-dashed p-16 text-center text-muted-foreground">لا توجد منتجات في هذا التصنيف بعد.</p> : null}</main>; }
